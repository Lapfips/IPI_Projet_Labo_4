POSTGRES_LISTEN_ADDRESSES: "0.0.0.0"
POSTGRES_HBA_RULES:
  - type: host
    database: all
    user: all
    address: 192.168.125.0/24
    method: md5
  - type: host
    database: all
    user: all
    address: 192.168.150.0/24
    method: md5
POSTGRES_USERS:
  - name: guacamole
    password: "{{ vault_guacamole_db_password }}"
  - name: glpi
    password: "{{ vault_glpi_db_password }}"
  - name: nextcloud
    password: "{{ vault_nextcloud_db_password }}"
  - name: zabbix
    password: "{{ vault_zabbix_db_password }}"
POSTGRES_DATABASES:
  - name: guacamole_db
    owner: guacamole
  - name: glpi
    owner: glpi
  - name: nextcloud
    owner: nextcloud
  - name: zabbix
    owner: zabbix
